# Advent of Code 2025 — Rust

## Running

```sh
cargo run --bin day01 -- --input path/to/input.txt
cargo run --bin day01 -- --input path/to/input.txt --part 1
```

Replace `day01` with any `dayNN` bin. `--part` is optional; omitting it runs both parts.
